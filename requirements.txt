# Core ML
torch>=2.1.0
torchvision>=0.16.0

# ONNX bridge
onnx>=1.16.0
onnxruntime-gpu>=1.17.0
onnxscript>=0.1.0   # required by torch.onnx.export on PyTorch 2.5+

# TensorRT (Linux + NVIDIA only — installs no-op on macOS, see README)
tensorrt>=10.0.0
pycuda>=2024.1

# Engine inspection / fusion analysis
polygraphy>=0.49.0

# Data + validation
numpy>=1.24.0
pillow>=10.0.0
datasets>=2.18.0
tqdm>=4.66.0

# Benchmarking + visualization
matplotlib>=3.8.0
pandas>=2.1.0
